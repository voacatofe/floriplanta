/** @type {import('next').NextConfig} */
const nextConfig = {
  // Comentado durante desenvolvimento
  // output: 'export',
  // distDir: 'out',
  images: {
    unoptimized: true
  }
}

module.exports = nextConfig 